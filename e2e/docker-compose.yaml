version: "3.8"

services:
  proxy:
    image: saucelabs/forwarder:${FORWARDER_VERSION}
    healthcheck:
      interval: 1s
      timeout: 250ms
      retries: 10

  upstream-proxy:
    extends: proxy

  httpbin:
    extends: proxy
    command: httpbin
