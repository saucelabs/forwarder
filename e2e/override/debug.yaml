version: "3.8"

services:
  proxy:
    environment:
      FORWARDER_LOG_HTTP: "headers"
      FORWARDER_LOG_LEVEL: "debug"

  upstream-proxy:
    environment:
      FORWARDER_LOG_HTTP: "headers"
      FORWARDER_LOG_LEVEL: "debug"
