version: "3.8"

services:
  proxy:
    environment:
      FORWARDER_LOG_HTTP: "headers"
      FORWARDER_VERBOSE: "true"

  upstream-proxy:
    environment:
      FORWARDER_LOG_HTTP: "headers"
      FORWARDER_VERBOSE: "true"
