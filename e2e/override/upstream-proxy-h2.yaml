version: "3.8"

services:
  proxy:
    environment:
      FORWARDER_UPSTREAM_PROXY: "https://upstream-proxy:3128"
      FORWARDER_INSECURE_SKIP_VERIFY: "true"
  upstream-proxy:
    environment:
      FORWARDER_PROTOCOL: "H2"
