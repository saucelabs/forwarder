version: "3.8"

services:
  proxy:
    environment:
      FORWARDER_PROXY: "https://upstream-proxy:3128"
      FORWARDER_INSECURE: "true"
  upstream-proxy:
    environment:
      FORWARDER_PROTOCOL: "HTTPS"
